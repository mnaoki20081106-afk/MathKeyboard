name: Build MathKeyboard IPA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.app

      - name: Show Xcode version
        run: xcodebuild -version

      # SPMキャッシュ（ビルド時間短縮）
      - name: Cache SPM
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData
            .build
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-

      # 署名なしでビルド
      - name: Build (unsigned)
        run: |
          xcodebuild \
            -project MathKeyboard.xcodeproj \
            -scheme MathKeyboardApp \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath "${{ github.workspace }}/DerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            EXPANDED_CODE_SIGN_IDENTITY="" \
            | xcpretty || true

      # ビルド結果を確認
      - name: Find .app
        run: |
          echo "=== DerivedData 以下の .app を検索 ==="
          find "${{ github.workspace }}/DerivedData" -name "*.app" -type d

      # .app を Payload に入れてIPA化
      - name: Package IPA
        run: |
          APP_PATH=$(find "${{ github.workspace }}/DerivedData" -name "MathKeyboardApp.app" -type d | grep -v "PackageFrameworks" | head -1)
          echo "Found .app at: $APP_PATH"
          mkdir -p Payload
          cp -r "$APP_PATH" Payload/MathKeyboardApp.app
          zip -r MathKeyboard.ipa Payload
          echo "IPA size: $(ls -lh MathKeyboard.ipa)"

      # IPAをアーティファクトとしてアップロード
      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: MathKeyboard-unsigned
          path: MathKeyboard.ipa
          retention-days: 30
